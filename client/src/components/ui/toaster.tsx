import React from "react";

// Simple placeholder toaster that doesn't conflict with our new toast system
export function Toaster() {
  return null; // Our new toast system is self-contained and doesn't need this
}
